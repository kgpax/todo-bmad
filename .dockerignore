# OS artifacts
.DS_Store
**/.DS_Store
*.log
**/*.log

# Dependencies — reinstalled inside the container
node_modules
**/node_modules

# Build artifacts — rebuilt inside the container
.next
**/dist

# Database files — mounted via Docker volume, not baked into image
data
**/data

# Test artifacts
coverage
**/coverage
test-results
playwright-report

# Secrets — passed via docker-compose environment
.env
.env.local
.env.development
.env.production
.env.*.local
**/.env
**/.env.local
**/.env.development
**/.env.production
**/.env.*.local

# Version history
.git

# E2E tests — not needed in production images
e2e

# Planning artifacts — not needed in production images
_bmad
_bmad-output

# Documentation — **/*.md ensures subdirectory .md files are also excluded
**/*.md
